import { useGetWeatherQuery } from "./api/api";
import favoritesReducer from "./favorites/api/favoritesSlice";
import historyReducer from "./history/api/historySlice";

export { useGetWeatherQuery, favoritesReducer, historyReducer };
